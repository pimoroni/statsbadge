name: Publish clock

# Publishes statsbadge-clock to PyPI from a vN.N.N release tag, alongside statsbadge. The
# release scheme, the trusted-publishing setup and why each package has a workflow file to
# itself are under Packaging in DEVELOPMENT.md.
on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    if: >
      github.event_name == 'workflow_dispatch'
      || startsWith(github.event.release.tag_name, 'v')
    uses: ./.github/workflows/extension-build.yml
    with:
      directory: extensions/statsbadge-clock

  publish:
    name: Publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write # for trusted publishing

    steps:
      - uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist

      # setup-uv publishes exact versions only, no major tag
      - uses: astral-sh/setup-uv@v10.2.0
        with:
          version: "0.12.18"

      - run: uv publish --trusted-publishing always dist/*
